package coding

func InterpretArithmeticCommand(commands []string) (output float64) {
	/*
		// Code start here

		var add float64
		var multiply float64
		var substract float64

		var addoutput float64
		var multiplyoutput float64
		var substractoutput float64
		var devideoutput float64

		for i := 0; i < len(commands); i++ {

			input1 := []string{"add", "multiply", "substract", "devide"}

			var input2 int

			switch commands {
			case "add":
				for i := add; i < add; i++ {
					addoutput = i + i
					return addoutput
				}

			case "multiply":
				for i := multiply; i < multiply; i++ {
					multiplyoutput = i * i
					return multiplyoutput
				}

			case "substract":
				for i := substract; i < substract; i++ {
					substractoutput = i - i
					return substractoutput
				}

			case "devide":
				for i := devide; i < devide; i++ {
					devideoutput = i / i
					return devideoutput
				}

			}

			finalOutput := addoutput + multiplyoutput + substractoutput

		}
	*/

	return
}
